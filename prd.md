# AutoCAD 2024 CMD → Python 重构总计划

## 1. 项目目标

本项目的目标不是把现有 CMD 安装器逐行翻译成 Python，也不要求 Python 与 CMD 产生 1:1 完全一致的副作用。

核心目标是：

> **完整理解现有 CMD 及其配套资源承担了哪些实际功能，在此基础上重新设计和实现一个结构清晰、可维护、可测试的 Python 安装器。**

CMD 是：

* 功能发现来源；
* 历史实现参考；
* 兼容性参考；

但**不是绝对正确的产品规范**。

如果 CMD 中存在：

* 明显 bug；
* 历史遗留行为；
* 无意义副作用；
* 危险操作；
* CMD 语法导致的偶然行为；

Python 不需要照搬。

---

# 2. 当前阶段原则

当前阶段以 **MVP 可用性** 为第一目标。

优先级：

1. 搞懂原 CMD。
2. 搞清楚真正产品功能。
3. Python 把主要功能实现出来。
4. 真实验证能够正常使用。
5. 再逐步完善异常处理、安全、事务、回滚等能力。

当前阶段不要过度设计：

* 完整事务系统；
* 极端并发保护；
* hostile archive 防御；
* 企业级安全模型；
* 复杂权限框架；
* 为所有理论异常建立恢复机制。

但明显危险且没有必要复制的 CMD 行为仍然不应主动照搬。

---

# 3. 总体方法

整个工作分成五个阶段。

---

# Phase 1：完整理解 CMD

这一阶段原则上**不急着修改 Python**。

目标：

> 把 CMD 从“500 行脚本”还原成一份可以理解的产品功能说明书。

需要一起分析：

* 主 CMD；
* 所有 label / CALL；
* `配置.txt`；
* `reg*.dli`；
* 其他 `.dli`；
* `app/*.dll` 实际压缩包内容；
* `guanfang.dll`；
* `default.dll`；
* `Tohomedrive.dll`；
* `VBA.dll`；
* WebView2 相关归档；
* 外部 EXE / DLL / helper；
* shortcut；
* junction；
* uninstall / recovery；
* 自定义配置。

## Phase 1 产物

建立：

`CMD_FUNCTIONAL_SPEC.md`

不要按 CMD 行号机械记录，而是按产品功能分类。

例如：

### F01 环境准备

说明：

* CMD 做什么；
* 为什么做；
* 输入是什么；
* 最终结果是什么；
* 依赖什么资源；
* 是否属于真实产品需求。

### F02 AutoCAD 注册

### F03 CHS / 中文资源

### F04 用户配置

### F05 VBA

### F06 WebView2

### F07 Shell / AcSign

### F08 Shortcut

### F09 Junction

### F10 自定义配置

### F11 卸载

### F12 Repair / Recovery

……

要求：

> 原 CMD 中所有有意义的安装行为最终都必须能够归入某个功能。

允许存在：

`UNKNOWN`

但 Phase 1 结束时原则上不应再有无法解释的重要行为。

---

# Phase 2：产品功能取舍

Phase 1 搞懂以后，不直接开始照抄。

对每个功能做一次产品判断。

分为三类：

## A. Python 必须实现

真正影响用户使用的产品功能。

例如：

* AutoCAD 正常启动；
* CHS；
* Sample.cus；
* 注册表；
* VBA；
* Shortcut；
* Junction；
* 自定义配置；
* 必要依赖；
* 卸载；
* 修复。

---

## B. 不需要复制

属于 CMD 实现细节，而不是用户功能。

例如可能包括：

* 临时 `cad.log`；
* 临时 `1.ps1`；
* CMD title；
* 中间变量；
* 某些日志文件；
* 为 CMD 语法服务的辅助文件。

---

## C. Python 应重新设计

CMD 虽然做了，但做法本身不合理。

例如：

* 整树 `REG DELETE`；
* 无条件 `-aoa` 覆盖 System32；
* 强制结束所有 AutoCAD；
* 大面积删除用户 AppData；
* CMD `&&` / `&` 语义造成的意外无条件执行；
* 注册了组件但没有保证对应文件存在。

这些功能如果确实需要，应保留“功能目的”，重新设计实现方式。

---

## Phase 2 产物

建立：

`PYTHON_FUNCTIONAL_REQUIREMENTS.md`

每个功能记录：

* 功能名称；
* 是否需要；
* 用户看到什么；
* 最终系统状态；
* Python 推荐实现；
* 是否属于 MVP；
* 是否需要后续增强。

到这里以后，**真正的 Python 产品规范以这个文件为准，而不再以 CMD 为准。**

---

# Phase 3：重新设计 Python MVP

在前两阶段完成以后，重新审视现有 Python 项目。

不要因为已有代码存在就强行保留。

判断：

* 可以继续用；
* 需要重构；
* 应删除；
* 应重新实现。

建议最终结构围绕产品职责，而不是 CMD 行号：

```text
manager.py

deploy/
    autocad.py
    registry.py
    chs.py
    vba.py
    webview.py
    shortcuts.py
    junctions.py
    custom_config.py

uninstall/
repair/

platform/
    windows.py
```

具体结构允许实施 AI 自主调整，不要求现在提前固定。

---

# 4. MVP 实现顺序

优先完成主线，不要同时处理所有边角功能。

建议顺序：

## M1 基础安装骨架

实现：

* 配置读取；
* 路径确定；
* 基础目录；
* AutoCAD 主路径；
* 基础注册表。

验收：

AutoCAD 主体能够正确识别。

---

## M2 CHS / 基础资源

包括：

* CHS；
* `Sample.cus`；
* 中文资源；
* 必要配置恢复。

重点：

只实现真实功能，不要求复制 CMD 的每一个恢复过程。

---

## M3 Shortcut / Junction / 自定义配置

这些是相对独立且容易验证的功能。

---

## M4 VBA

目标不是“CMD 解包成功”，而是：

> 用户真正可以使用 AutoCAD VBA。

至少关注：

* VBA Bundle；
* AcVBA.arx；
* 必要运行库；
* 必要注册；
* `VBALOAD`；
* `VBAIDE`；
* 普通宏。

UserForm / Forms 2.0 是否属于 MVP，在理解完整依赖以后决定。

---

## M5 WebView2 / Shell 等外部依赖

先理解用途，再决定：

* Python 自己部署；
* 检测系统已有；
* 使用官方 Runtime；
* 或 MVP 暂不支持。

不要因为 CMD 包里有东西就默认全部复制。

---

## M6 卸载 / Repair

MVP 先做到：

* 能删除自己创建的主要内容；
* 不破坏明显的用户数据；
* 能恢复最核心状态。

复杂事务式回滚以后再增强。

---

# 5. 测试原则

以后不再把：

> CMD 每个副作用是否完全一致

作为主要验收标准。

测试重点转成：

## 功能测试

例如：

* AutoCAD 能否正常启动；
* CHS 是否正常；
* VBA 是否可用；
* shortcut 是否正常；
* junction 是否正确；
* 注册表是否指向真实文件；
* 自定义配置是否生效。

## 状态测试

例如：

* 必需文件存在；
* 注册表无明显悬空；
* 路径正确；
* 重复运行不会明显破坏已有安装。

## 回归测试

修复一个功能后不能破坏其他已完成功能。

---

# 6. CMD 差分审计的新定位

现有 CMD Gate / behavior matrix 不删除。

但是以后它属于：

> **辅助发现工具**

而不是：

> **Python 产品完成度的最高判定标准**

它主要用于发现：

* 是否还有 CMD 行为没有被理解；
* 是否漏掉某个资源；
* 是否漏掉某个明显产品功能。

如果发现差异，处理流程为：

```text
发现 CMD 差异
↓
判断这个行为目的是什么
↓
是否属于产品需求？
├─ 否 → 明确不实现
└─ 是
   ↓
Python 是否已有合理实现？
├─ 是 → 无需复制 CMD
└─ 否 → 补功能
```

---

# 7. 完成标准

以后判断“Python 重构完成”，不再要求：

> CMD 与 Python 所有副作用完全一致。

而要求：

### 功能理解完成

CMD 重要行为和资源均已被解释。

### 产品功能确定

所有真实需要的功能均有明确需求。

### Python MVP 完成

主要功能全部实现。

### 真实运行可用

AutoCAD 能够正常运行，核心功能正常。

### 无重大未知项

不能再出现：

> “完全不知道这个压缩包 / 注册表 / CALL 是干什么的。”

允许存在：

* 已理解但明确不支持；
* 已理解但留到 V2；
* 已理解且认为不应复制。

---

# 8. 关于安全

当前阶段：

> **安全不是最高优先级，但不能主动复制明显危险行为。**

MVP 可以暂时不实现：

* 完整事务；
* 全面 rollback；
* 并发安全；
* 极端恶意输入防御。

但以下操作必须保持谨慎：

* System32；
* SysWOW64；
* HKLM；
* 整树注册表删除；
* 用户 AppData 删除；
* 覆盖已有 DLL；
* 强杀进程。

这些需要有明确产品理由。

---

# 9. 开发流程

以后按照以下固定流程：

```text
CMD / 资源阅读
        ↓
理解产品功能
        ↓
更新 CMD_FUNCTIONAL_SPEC.md
        ↓
做产品取舍
        ↓
更新 PYTHON_FUNCTIONAL_REQUIREMENTS.md
        ↓
实现 Python
        ↓
功能测试
        ↓
回归
        ↓
独立审计
```

禁止重新退化成：

```text
发现一行 CMD
↓
立即补一行 Python
```

---

# 10. 当前下一步

从头开始执行 **Phase 1：完整理解 CMD**。

暂时停止：

* INS-21c 深挖；
* Forms 2.0 真机部署；
* F:/D: 环境折腾；
* System32 功能补齐；
* 为减少 MISSING 数字而开发。

第一阶段只做：

> **CMD + 配置 + 注册表模板 + 所有资源包的完整功能解剖。**

最终形成：

`CMD_FUNCTIONAL_SPEC.md`

等我们真正知道整个 CMD 是一个什么东西，再开始决定新的 Python 应该是什么东西。
