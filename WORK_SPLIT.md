# 工作分工：主 AI / 本地 AI

## 总原则

本项目采用“主 AI 负责判断与集成，本地 AI 负责批量机械工作”的分工。

目标：

- 保持产品方向、架构和验收口径一致；
- 避免主 AI 单独完成所有扫描/盘点导致会话超时；
- 避免本地 AI 自主扩大范围、边发现问题边顺手改系统。

## 主 AI 负责

- 阅读并理解主 CMD 的整体控制流；
- 维护 `CMD_FUNCTIONAL_SPEC.md`；
- 判断哪些行为属于产品功能、历史实现细节或明显缺陷；
- Phase 2 编写 `PYTHON_FUNCTIONAL_REQUIREMENTS.md`；
- Python 架构设计和主要实现；
- 测试设计、回归、Git 集成和最终验收；
- 对本地 AI 返回的证据进行复核和产品解释。

## 本地 AI 负责

本地 AI 优先承担耗时但判断性较弱的工作：

- 批量读取/分类 `.dli` 注册表模板；
- 统计 registry root、文件路径引用、COM/CLSID/TypeLib 引用；
- 只读列出伪装为 DLL 的 7z 归档内容；
- 为每个资源包生成“成员清单 + 目标路径 + 被 CMD 哪处消费”的原始报告；
- 搜索 CMD/模板之间的引用关系；
- 运行只读分析脚本和普通测试；
- 整理原始证据和异常项。

## 本地 AI 不得自主做

- 不决定 Python 产品需求；
- 不把“CMD 做了”直接等价为“Python 必须做”；
- 不修改 Python 生产代码；
- 不修改 System32 / SysWOW64 / HKLM；
- 不运行旧 CMD 安装/卸载/恢复；
- 不启动 AutoCAD 做真机实验；
- 不因为发现新问题而扩展到其他模块；
- 不自行把分析结果标为最终 PASS / COVERED。

## 交接格式

本地 AI 每次只提交：

1. 扫描范围；
2. 实际读取了哪些文件；
3. 原始统计和清单；
4. 明确可证明的事实；
5. 不确定/无法判断项；
6. 生成的证据文件路径；
7. 是否修改过任何文件（正常应为否，除 evidence/report）。

主 AI收到后再决定如何写入产品规范。

